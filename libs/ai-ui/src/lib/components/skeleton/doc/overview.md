# Skeleton

O `[ai-skeleton]` é um componente utilizado para exibir placeholders de carregamento, ajudando a indicar ao usuário que o conteúdo ainda está sendo processado ou carregado. Não utiliza nenhuma biblioteca externa.
