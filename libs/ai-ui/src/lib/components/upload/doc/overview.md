# Upload

O `<ai-upload>` é um componente para enviar arquivos. Suporta dois modos: botão (padrão) e dropzone (arrastar e soltar). Também possui o componente auxiliar `<ai-upload-selected>` para exibir os arquivos selecionados. Não utiliza nenhuma biblioteca externa.
