# Carousel

O `<ai-carousel>` é um componente de slideshow para alternar entre elementos com suporte a arrastar com o mouse, deslizar por toque e reprodução automática. Não utiliza nenhuma biblioteca externa.
