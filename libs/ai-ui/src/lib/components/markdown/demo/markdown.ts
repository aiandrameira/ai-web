import { DemoMarkdownViewerComponent } from "./viewer";

export const MARKDOWN = {
    componentName: "markdown",
    componentType: "markdown",
    examples: [{ name: "viewer", component: DemoMarkdownViewerComponent }],
};
