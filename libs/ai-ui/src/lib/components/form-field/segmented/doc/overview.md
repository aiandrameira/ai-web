# Segmented

O `ai-segmented` é um componente de controle segmentado que permite ao usuário selecionar uma opção entre várias. Ele foi projetado para uso em formulários Angular, integrando-se com `SignalsForms` através da implementação de `FormValueControl<string>`.
