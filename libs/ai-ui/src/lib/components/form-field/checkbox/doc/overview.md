# Checkbox

O `[ai-checkbox]` é um componente de interface que representa um controle booleano (marcado / desmarcado). Ele foi projetado para uso em formulários Angular, integrando-se com `SignalsForms` e `ngModel` através da implementação de `FormCheckboxControl`.
