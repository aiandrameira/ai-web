# Switch

O `ai-switch` é um componente de alternância (toggle) que permite ativar ou desativar uma opção. Ele foi projetado para uso em formulários Angular, integrando-se com `SignalsForms` através da implementação de `FormCheckboxControl`.
