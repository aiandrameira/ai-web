import { DemoDialogDefaultComponent } from "./default";

export const DIALOG = {
    componentName: "dialog",
    componentType: "dialog",
    examples: [
        {
            name: "default",
            component: DemoDialogDefaultComponent,
        },
    ],
};
