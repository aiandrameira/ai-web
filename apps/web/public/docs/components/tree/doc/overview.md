# Tree

O `[ai-tree]` é um componente de visualização hierárquica em árvore para exibir estruturas de dados aninhadas com suporte a expandir/recolher e seleção. Não utiliza nenhuma biblioteca externa.
