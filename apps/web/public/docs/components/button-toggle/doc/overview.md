# Button Toggle

O `[ai-button-toggle]` é um componente de interface que representa um conjunto de botões de alternância (toggle buttons) agrupados, permitindo seleção única ou múltipla. Ele foi projetado para uso em formulários Angular, integrando-se com `ReactiveForms` e `ngModel` através da implementação de `ControlValueAccessor`.
