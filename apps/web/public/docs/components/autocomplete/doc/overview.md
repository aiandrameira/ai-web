# Autocomplete

O `[ai-autocomplete]` é um componente de campo de formulário que oferece sugestões enquanto o usuário digita. Ele pode ser usado para busca em listas locais, facilitando a seleção de opções a partir de um conjunto de dados configurável. Foi projetado para ser utilizado com o `SignalForms` do Angular.
