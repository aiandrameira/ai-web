# Radio

O `ai-radio-group` e `ai-radio` são componentes que permitem ao usuário selecionar uma única opção entre várias. Foram projetados para uso em formulários Angular, integrando-se com `SignalsForms` através da implementação de `FormValueControl<T | null>`.
