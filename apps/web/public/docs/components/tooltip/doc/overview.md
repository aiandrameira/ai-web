# Tooltip

O `[ai-tooltip]` é uma diretiva que adiciona uma dica de ferramenta (tooltip) a um elemento HTML. Ele exibe informações adicionais quando o usuário passa o mouse sobre o elemento ou foca nele.
