# Datetime

O `ai-datetime` é um componente de input para seleção de datas e horários. Ele suporta diferentes tipos de input (`date`, `time`, `month`, `datetime-local`) e integra-se com `SignalsForms` através da implementação de `FormValueControl<string>`.
