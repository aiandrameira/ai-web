# Input

O `[ai-input]` é um componente de campo de entrada que oferece suporte a diferentes variantes de estilo, máscaras de entrada e funcionalidades adicionais, como ícones interativos. Ele foi projetado para ser utilizado com o `SignalForms` do Angular.
